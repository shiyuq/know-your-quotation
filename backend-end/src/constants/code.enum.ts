export enum UserStatus {
  InValid = 0,
  Valid = 1,
}

export enum ProductStatus {
  InValid = 0,
  Valid = 1,
}

export enum ProductImportStatus {
  ValidString = '在售',
  InValidstring = '下架',
}
